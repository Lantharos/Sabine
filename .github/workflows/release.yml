name: Release Sabine

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            target: windows-x86_64
            archive: zip
          - os: macos-15
            target: macos-aarch64
            archive: tar.gz
          - os: ubuntu-24.04
            target: linux-x86_64
            archive: tar.gz
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
      - name: Validate release tag
        if: github.event_name == 'push'
        shell: pwsh
        run: |
          $content = Get-Content Cargo.toml -Raw
          if ($content -notmatch '(?ms)\[workspace\.package\].*?^version\s*=\s*"([^"]+)"') {
            throw "workspace version is missing"
          }
          $publicVersion = $Matches[1] -replace '\.0$', ''
          if ($env:GITHUB_REF_NAME -ne "v$publicVersion") {
            throw "tag $env:GITHUB_REF_NAME does not match public workspace version v$publicVersion"
          }
      - name: Install Linux build dependencies
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install --yes libwayland-dev libfontconfig1-dev libxkbcommon-dev libx11-dev libgtk-3-dev libnss3-dev libasound2-dev libgbm-dev ninja-build
      - run: rustup toolchain install 1.90 --profile minimal
      - run: rustup override set 1.90
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: release-${{ matrix.target }}
          cache-on-failure: true
      - run: cargo build --release -p sabine-cli -p sabine-service
      - name: Verify hidden Windows process creation
        if: runner.os == 'Windows'
        run: cargo test --release -p sabine-runtime process::tests::background_children_do_not_inherit_a_console -- --exact
      - name: Verify self-contained Windows binaries
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          foreach ($name in @('sabine.exe', 'sabine-service.exe', 'sabine-service-daemon.exe')) {
            $path = Join-Path 'target/release' $name
            $headers = (& llvm-objdump --private-headers $path 2>&1 | Out-String)
            if ($LASTEXITCODE -ne 0) {
              throw "llvm-objdump could not inspect $name"
            }
            if ($headers -match '(?i)(VCRUNTIME|MSVCP|api-ms-win-crt)') {
              throw "$name still depends on the redistributable C runtime"
            }
          }
          $daemonHeaders = (& llvm-objdump --private-headers 'target/release/sabine-service-daemon.exe' 2>&1 | Out-String)
          if ($daemonHeaders -notmatch '(?i)Subsystem\s+00000002\s+\(Windows GUI\)') {
            throw 'sabine-service-daemon.exe is not marked as a GUI application'
          }
          $env:LOCALAPPDATA = Join-Path $env:RUNNER_TEMP 'sabine-daemon-smoke'
          $daemon = Start-Process 'target/release/sabine-service-daemon.exe' -PassThru -WindowStyle Hidden
          Start-Sleep -Seconds 3
          if ($daemon.HasExited) {
            throw "sabine-service-daemon.exe exited during startup with code $($daemon.ExitCode)"
          }
          Stop-Process -Id $daemon.Id -Force
      - name: Prepare stable-ABI CEF host
        shell: pwsh
        run: |
          $hostPath = cargo run --release -p sabine-cli -- runtime prepare | Select-Object -Last 1
          if ($LASTEXITCODE -ne 0) { throw 'Native CEF host preparation failed' }
          "SABINE_HOST_PATH=$hostPath" >> $env:GITHUB_ENV
      - name: Stage system and CLI
        shell: pwsh
        run: |
          $suffix = if ($IsWindows) { ".exe" } else { "" }
          New-Item -ItemType Directory -Force "release/system"
          Copy-Item "target/release/sabine-service$suffix" "release/system/"
          Copy-Item "target/release/sabine-service-daemon$suffix" "release/system/"
          if ($IsMacOS) {
            if ($env:SABINE_HOST_PATH -notmatch '^(.+\.app)(?:[/\\].*)?$') {
              throw "macOS Sabine host is not inside an app bundle"
            }
            $hostBundle = $Matches[1]
            if (-not (Test-Path -LiteralPath $hostBundle -PathType Container)) {
              throw "macOS Sabine host app bundle does not exist"
            }
            Copy-Item -LiteralPath $hostBundle "release/system/" -Recurse
            $stagedHostBundle = "release/system/sabine-host.app"
            $env:SABINE_HOST_PATH = (Resolve-Path "$stagedHostBundle/Contents/MacOS/sabine-host").Path
          } else {
            Copy-Item $env:SABINE_HOST_PATH "release/system/"
            if ($IsWindows) {
              Copy-Item ([IO.Path]::ChangeExtension($env:SABINE_HOST_PATH, '.dll')) "release/system/"
              Copy-Item (Join-Path (Split-Path $env:SABINE_HOST_PATH) 'chrome_elf.dll') "release/system/"
            }
            $env:SABINE_HOST_PATH = (Resolve-Path "release/system/sabine-host$suffix").Path
          }
          "SABINE_HOST_PATH=$env:SABINE_HOST_PATH" >> $env:GITHUB_ENV
          Copy-Item "target/release/sabine$suffix" "release/"
      - name: Permit the staged Linux host sandbox under AppArmor
        if: runner.os == 'Linux'
        run: |
          target/release/sabine runtime sandbox-profile > sabine-apparmor
          sudo install -m 644 sabine-apparmor /etc/apparmor.d/sabine-release
          sudo apparmor_parser -r /etc/apparmor.d/sabine-release
      - name: Validate the staged Chromium runtime
        shell: pwsh
        run: |
          $suffix = if ($IsWindows) { '.exe' } else { '' }
          & "release/sabine$suffix" runtime doctor --json
          if ($LASTEXITCODE -ne 0) { throw 'The staged CEF host could not render and shut down' }
      - name: Package assets
        shell: pwsh
        run: |
          if ($IsWindows) {
            Compress-Archive -Path "release/system/*" -DestinationPath "release/sabine-system-${{ matrix.target }}.zip"
            Compress-Archive -Path "release/sabine.exe" -DestinationPath "release/sabine-cli-${{ matrix.target }}.zip"
          } else {
            tar -C release/system -czf "release/sabine-system-${{ matrix.target }}.tar.gz" .
            if ($LASTEXITCODE -ne 0) { throw 'System archive creation failed' }
            tar -C release -czf "release/sabine-cli-${{ matrix.target }}.tar.gz" sabine
            if ($LASTEXITCODE -ne 0) { throw 'CLI archive creation failed' }
          }
      - uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.target }}
          path: |
            release/sabine-system-*
            release/sabine-cli-*
          if-no-files-found: error

  publish:
    if: github.event_name == 'push'
    needs: build
    runs-on: ubuntu-24.04
    env:
      SABINE_UPDATE_SIGNING_KEY: ${{ secrets.SABINE_UPDATE_SIGNING_KEY }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v8
        with:
          path: release
          merge-multiple: true
      - name: Install CLI runtime dependencies
        run: sudo apt-get update && sudo apt-get install --yes libfontconfig1
      - name: Generate release metadata
        run: |
          mkdir -p "$RUNNER_TEMP/sabine-release-cli"
          tar -xzf release/sabine-cli-linux-x86_64.tar.gz -C "$RUNNER_TEMP/sabine-release-cli"
          "$RUNNER_TEMP/sabine-release-cli/sabine" system-release-manifest --version "$GITHUB_REF_NAME" --directory release --output release/sabine-release.json
      - uses: actions/attest@v4
        with:
          subject-path: release/*
      - name: Publish immutable release candidate
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python3 scripts/release_notes.py "$GITHUB_REF_NAME" > "$RUNNER_TEMP/sabine-release-notes.md"
          gh release create "$GITHUB_REF_NAME" --draft --verify-tag --notes-file "$RUNNER_TEMP/sabine-release-notes.md" release/*
          gh release edit "$GITHUB_REF_NAME" --draft=false --latest=false
